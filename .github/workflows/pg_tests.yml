name: PGTap Tests
on:
  pull_request:
    branches: [ main ]
jobs:
  pg_tap_tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/workflows/build.yml
      - name: Run Tests
        run: supabase test db