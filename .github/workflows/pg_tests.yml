name: PGTap Tests
on:
  pull_request:
    branches: [ main ]
jobs:
  pg_tap_tests:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: ./.github/workflows/build.yml
      - name: Run Tests
        run: supabase test db